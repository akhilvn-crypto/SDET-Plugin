---
description: Run the SDET pipeline for one approved test case — UI by default, API-only or balanced (frontend + API) when explicitly requested — write -> execute -> route to bug-reporter or code-reviewer.
argument-hint: <approved test case id or description> [--type ui|api|balanced] [--collection <path to Postman/OpenAPI/Insomnia file>]
---

Invoke the **automate** skill (`skills/automate/SKILL.md`) with these arguments: $ARGUMENTS
